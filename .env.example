# Django Settings
SECRET_KEY=django-insecure-$5^p1jdkuc-wnvk+b3sy20#f)2&6w)e61-*-%xb(0fb*96p5wy
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# For MySQL (uncomment and configure if needed)
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=alx_travel_db
DATABASE_USER=root
DATABASE_PASSWORD=root
DATABASE_HOST=localhost
DATABASE_PORT=3306

# Email Settings (for future use)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# API Keys (for future use)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# Redis Settings (for Celery, if needed)
REDIS_URL=redis://localhost:6379/0

# AWS Settings (for file uploads, if needed)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1


CHAPA_TEST_API_KEY=chapa-api-key
CHAPA_TEST_SECRET_KEY=chapa-secret
CHAPA_TEST_ENCRYPTION_KEY=chapa-encryption-key

CELERY_BROKER_URL = your-broker-url
CELERY_RESULT_BACKEND = rpc://
CELERY_ACCEPT_CONTENT = ['json']
CELERY_TASK_SERIALIZER = 'json'
CELERY_RESULT_SERIALIZER = 'json'
CELERY_TIMEZONE = 'UTC'
CELERY_ENABLE_UTC = True

EMAIL_BACKEND = your-email-backend
EMAIL_HOST = 'smtp.alx_travel_app.com'
EMAIL_PORT = your-email-port
EMAIL_USE_TLS = True
EMAIL_HOST_USER = your-email-host-user
EMAIL_HOST_PASSWORD = your-email-password
DEFAULT_FROM_EMAIL =  from-email